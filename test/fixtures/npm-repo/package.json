{
  "name": "npm-fixture",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc",
    "build:prod": "tsc -p tsconfig.prod.json",
    "test": "vitest run",
    "lint": "eslint ."
  },
  "devDependencies": {
    "vitest": "^2.1.0",
    "eslint": "^9.0.0"
  }
}
