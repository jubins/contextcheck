[project]
name = "demo"
version = "0.1.0"

[project.scripts]
demo-cli = "demo.cli:main"
serve = "demo.server:run"

[tool.poetry.scripts]
migrate = "demo.db:migrate"

[tool.hatch.envs.default.scripts]
test = "pytest"
lint = "ruff check ."

[tool.black]
line-length = 88
